<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        form {
            background-color: #fff;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0px 0px 10px 0px rgba(0,0,0,0.1);
        }
        h2, h3 {
            text-align: center;
            color: #333;
        }
        input[type="text"], textarea {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border-radius: 5px;
            border: 1px solid #ddd;
        }
        input[type="submit"] {
            width: 100%;
            padding: 10px;
            border-radius: 5px;
            border: none;
            color: #fff;
            background-color: #FF5722;
            cursor: pointer;
        }
         input[type="submit"]:hover {
            background-color: #E64A19;
        }
        a {
            display: block;
            text-align: center;
            margin-top: 10px;
            color: #FF5722;
            text-decoration: none;
        }
        form {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        label {
            margin-bottom: 5px;
        }

        input[type="text"],
        input[type="file"],
        textarea,
        input[type="submit"] {
            margin-bottom: 15px;
        }
     a.button {
            display: inline-block;
            padding: 10px 20px;
            color: #fff;
            background-color: #3F51B5;
            border-radius: 5px;
            text-decoration: none;
            text-align: center;
            transition: background-color 0.3s ease;
        }

        a.button:hover {
            background-color: #303F9F;
        }
    </style> 
</head>
<body>
    <form action="/upload" method="post" enctype="multipart/form-data">
        <label>Product Name: </label>
        <input type="text" name="productName" value="<%=data.product %>" required>

        <label>Product Image:</label>
        <input type="file" name="productImage" value="<%=data.image%>" required>

        <label >Product Description:</label>
        <textarea  name="productDescription" rows="4" cols="50" required> <%=data.description%></textarea>

        <input type="submit" value="Submit">
        <a href="/blog">Go to Blog</a>
    </form>
</body>
</html>